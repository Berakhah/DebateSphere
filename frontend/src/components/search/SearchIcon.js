import React from 'react';
import searchIconUrl from '../../assets/search.svg'; // Import as a URL

const SearchIcon = () => (
  <img src={searchIconUrl} alt="Search" />
);

export default SearchIcon;
